<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head") %>


<body>

    <%- include("../partials/welcome") %>

    <%- include("../partials/header") %>


    <main>
        <h2 class="main">INICIO DE SESIÓN</h2>
        <form action="/Usuarios/login" method="POST" class="formInicioSesion">

                <div class="camposInicioSesion">
                    <label for="email">Ingresá tu correo electrónico:</label>
                    <input type="email" 
                        id="email" 
                        name="email" 
                        class="inputcontrol" 
                        value="<%= locals.oldData ? oldData.email : null %>"
                    >
                        <% if(locals.errors && errors.email){ %>
                            <div class="errorValidation"><%= errors.email.msg %> </div>
                        <% } %> 
                </div>
            
                <div class="camposInicioSesion">
                    <label for="contraseña">Ingresá tu contraseña:</label>
                    <input type="password" 
                    name="password" 
                    id="contraseña" 
                    class="inputcontrol" 
                    minlength="6"
                    value="<%= locals.oldData ? oldData.password : null %>"
                >
                    <% if(locals.errors && errors.password){ %>
                        <div class="errorValidation"><%= errors.password.msg %> </div>
                    <% } %>

                </div>
            
                
                <div class="optionLogin">
                    <label>Recordar mi cuenta</label>
                    <input type="checkbox" name="remember_user">
                </div>
        
                <div class="optionLogin"><label>¿Olvidaste tu contraseña? <a href="#">Recuperala</a></label></div>

                <div class="contenedorButton"><button type="submit" class="buttonform">Iniciar Sesión</button></div>

                <p class="optionLogin">Si no tenés una cuenta, crea una haciendo click <a href="http://localhost:3000/Usuarios/Registrarse">acá</a></p>
        </form>


    </main>


    <%- include("../partials/footer") %>
    
</body>
</html>